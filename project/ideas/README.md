# Ideas

Feature ideas being developed before they become specs.

```
ideas/
└── I{n} {Idea name}.md
```

The process — the idea dialogue, the Definition of Ready, the `State` field — is defined in
[`../harness/_ideas.md`](../harness/_ideas.md). An idea is only turned into specs once its
state is `Ready` and the stakeholder has commissioned the work.

A template is in [`../harness/templates/`](../harness/templates/).

No ideas recorded yet. The first one starts at `I1`.
